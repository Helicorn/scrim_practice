# 프로젝트명: 내전 서포트용 페이지

## 1. 목표

- 리그 오브 레전드 **10인 내전**을 위한 서포트 페이지 제작

## 2. 구성

### 화면

| 화면 | 경로 (예정) | 설명 |
|------|-------------|------|
| 진입 | `/` 또는 `/setup` | 단판/시리즈 선택, 피어리스, 세션 시작 |
| 소환사 입력 | `/players` | 10인 소환사명·태그, 전적 검색(승률·모스트·포지션) |
| 팀 배치 | `/teams` | 레드/블루 5:5 구성 |
| 밴픽 | `/draft` | 챔피언 밴·픽 |
| 경기 결과 | `/result` | 승패·스탯 입력·저장 |

### DB

Oracle DDL 정리본: [`database/schema.sql`](./database/schema.sql) · 설명: [`database/README.md`](./database/README.md)

| 구분 | 테이블 |
|------|--------|
| 마스터 | `SUMMONER`, `CHAMPION` |
| 전적 | `SUMMONER_RANK_STAT` (랭크 fallback), `SUMMONER_CUSTOM_STAT`, `SUMMONER_CUSTOM_POSITION_STAT` (내전) |
| 세션·경기 | `CUSTOM_GAME`, `CUSTOM_GAME_PLAYER`, `CUSTOM_MATCH`, `CUSTOM_PICK_BAN`, `CUSTOM_MATCH_PLAYER_RESULT` |

- **전적 표시**: 내전 0판 → `SUMMONER_RANK_STAT`, 1판 이상 → `SUMMONER_CUSTOM_*`
- **세션**: `CUSTOM_GAME.SESSION_CODE` ↔ 프론트 `sessionId`

## 3. 화면 상세 구성

### 첫 페이지 (진입)

- **단판**, **3전 2선**, **5전 3선** 버튼으로 세션 유형 선택 후 다음 화면 진입
- 체크박스: **피어리스(Fearless)** 여부
- (권장) 세션 ID 발급 → 이후 모든 화면·DB가 동일 세션 참조

> **현재 구현:** `/` 진입 (단판·3전2선·5전3선·제한 없음, 피어리스). **이어서 진행**은 DB 스냅샷 조회 후 상태별 화면 이동. 세션 코드 입력으로 다른 브라우저에서도 복구. 삭제·새 내전 시작 시 서버 `CANCELLED`. localStorage는 같은 탭 새로고침용 캐시.

---

### 10인 소환사명 입력 페이지

- 10인의 **소환사명 + 태그** 입력
- **전적 검색**으로 승률, 모스트, 포지션 등 정보 조회 (Riot API, 백엔드 프록시)
- **전적 출처 정책**
  - **내전 전적이 없는 소환사** → **랭크 전적** 기준으로 승률·모스트·포지션 등 표시
  - **내전 전적이 1판이라도 생긴 소환사** → 이후부터는 **내전 성적** 기준으로 표시·갱신
  - (내전 경기는 Riot match-v5에 없으므로, 내전 성적은 **자체 DB 경기 히스토리**에서 집계)
- **첫 입력** 소환사 → DB 저장
- **이미 저장된** 소환사 → DB 조회 후 필요 시 갱신
- 개인 개발용 **Riot API Key** 입력 + [developer.riotgames.com](https://developer.riotgames.com/) 링크

> **현재 구현:** 10칸 입력 UI, API Key(localStorage), Riot Account 조회. **다음** 시 `POST .../players`로 Oracle 로스터·세션 저장, League-V4 랭크 갱신. 저장 목록에 내전/랭크 전적 배지·티어·승률·포지션·모스트. 랭크 fallback은 티어·승패 중심(모스트·주포지션 match-v5는 미구현).

---

### 10인 소환사 팀 배치 페이지

- **블루팀(좌)**, **레드팀(우)** 5:5
- **수동** 배치
- **자동** 배치: 플레이어 **점수**를 감안해 양팀 밸런스 (예: 현재 티어, **내전 승률** 등)  
  - 점수 산정·분할 알고리즘 필요 → 소환사 DB·경기 히스토리 연동 후 구현

> **현재 구현:** 좌(블루)·우(레드) 5칸, 클릭·드래그 수동 배치. **다음** 시 `PUT .../teams`로 `CUSTOM_GAME_PLAYER`에 팀·포지션 저장(`TEAM_SETUP`). 점수 기반 자동 배치 **미구현**.

---

### 밴픽 페이지

#### 공통

- **가운데**: 챔피언 초상화·선택 UI — 챔피언 메타·이미지는 **Data Dragon**
- **좌·우**: 팀별 **밴 5칸**, **픽 5칸** (좌=블루, 우=레드)
- 밴픽 순서: **블루 선밴 → 3밴 3픽 → 2밴 2픽** (마지막 픽 레드)
- 종료 후 **마감** / **다음 경기** 선택
- **피어리스**: 이전 경기에서 사용한 챔피언 비활성화 (진입 시 피어리스 ON인 경우)

#### 1안 — 제3자 진행 + 화면 공유

- 내전 참가자 **외부**인 제3자가 밴픽 페이지에서 진행
- 참가자에게 **화면 공유** (Discord 등)
- **실시간 동기화 불필요** (단일 클라이언트 조작)

#### 2안 — 팀별 URL + 팀장 진행 + 실시간

- 밴픽 URL에 **세션 ID** + **레드/블루** 구분
- 레드·블루 **팀장용 URL** 각각 전달 → 해당 팀 차례에만 조작
- **실시간**으로 모든 클라이언트에 밴픽 상태 반영 (WebSocket/SSE 등)
- 참가자에게 화면 공유 병행 가능

#### 모드 선택 (검토 중)

- 밴픽 페이지 **이전**에 1안 / 2안 (또는 둘 다 지원 시) 선택
- 구현 순서 권장: **공통 UI·상태 머신 → 1안 → 2안**

> **현재 구현:** DDragon 챔피언 검색·phase 상태 머신·피어리스 비활성·슬롯 스왑·밴픽 마감 시 `POST .../pick-ban` DB 저장(`RESULT_INPUT`). **1안**(단일 조작) 완료. 밴픽 **도중** 새로고침 시 그 판 밴픽은 다시 시작(마감분만 DB). **2안**(팀 URL·실시간) **미구현**.

---

### 경기결과 입력 페이지

- **승패**: 필수 (팀별 승리 체크)
- **그 외 스탯**: 선택 (KDA 등 수동 입력)
- **저장**: 소환사별 승/패 누적, 입력 시 KDA·사용 챔피언도 히스토리에 저장

> **현재 구현:** 승리 체크, K/D/A, 밴픽 초상화. **경기 결과 저장** → `POST .../match-result`로 Oracle 저장·`SUMMONER_CUSTOM_*` 재집계. localStorage 경기 히스토리는 보조. 시리즈 계속 시 다음 판 밴픽으로 이동.

---

## 4. 사용 기술

| 구분 | 기술 |
|------|------|
| 프론트 | Vue 3, TypeScript, Vite, Vue Router, Pinia |
| 백엔드 | Java Spring Boot 3 + JPA (Oracle) |
| DB | Oracle SQL |
| 외부 데이터 | [Riot API](https://developer.riotgames.com/) (소환사·전적), Data Dragon (챔피언·이미지) |
| 도구 | Cursor AI, GitHub |

## 5. 데이터·API 정리

| 용도 | 출처 |
|------|------|
| 챔피언 목록·초상화 | Data Dragon (CDN, API 키 불필요) |
| 소환사 조회 | Riot API (백엔드 프록시, 개발용 키 24h) |
| 랭크 전적(승률·모스트·포지션) | Riot match-v5 (내전 전적 없을 때 fallback) |
| 내전 전적(승률·모스트·포지션) | 자체 DB 경기 히스토리 집계 (내전 1판 이상 시 우선) |
| 밴픽·내전 결과 | 자체 DB (공식 내전 매치는 Riot 전적에 없음) |

## 6. 구현 현황

### 완료

- [x] Vue Router — `/`, `/players`, `/teams`, `/draft`, `/result`
- [x] Pinia 세션 + localStorage 캐시 (같은 브라우저 새로고침)
- [x] Spring Boot + Oracle — 소환사·세션·팀·밴픽·경기 결과 REST
- [x] 내전 전적 집계 (`SUMMONER_CUSTOM_*`) + 랭크 fallback (티어·승패)
- [x] 밴픽 1안 — DDragon, phase, 피어리스, DB 마감 저장
- [x] 시리즈(단판·BO3·BO5·제한 없음) + 피어리스 연속 경기
- [x] **세션 이어서 하기** — DB 스냅샷 조회, hydrate, 취소(`CANCELLED`)

### 미구현

- [ ] 팀 **점수 기반** 자동 배치
- [ ] 밴픽 **2안** (팀 URL, WebSocket 실시간)
- [ ] 밴픽 **도중** 턴 복구 (마감분만 저장 정책)
- [ ] 랭크 fallback **모스트·주포지션** (match-v5)
- [ ] API Key **서버 보관** (현재 브라우저 localStorage)
- [ ] Players 10칸 슬롯별 전적 한 줄 UI (저장 목록은 완료)

## 7. 권장 개발 순서 (남은 작업)

1. Players 10칸 전적 UI
2. 팀 점수 기반 자동 배치 (티어 → 내전 승률)
3. 랭크 fallback 모스트·주포지션 (match-v5)
4. API Key 서버 보관
5. 밴픽 **2안** (WebSocket)

## 8. 이슈·정책

### 세션 유지 (구현됨)

- **문제:** 새로고침·탭 이탈·다른 기기에서 진행 중 내전 데이터 소실
- **해결:** `SESSION_CODE`(프론트 `sessionId`) + Oracle 저장 + `GET /api/sessions/{code}` 복구
- **저장 시점:** 로스터 확정, 팀 배치, 밴픽 마감, 경기 결과 (확정 시점만 — 입력 중 KDA 등은 저장 안 함)
- **이어가기 화면:** Setup 「이어서 진행」 또는 세션 코드 입력 → `suggestedRoute`로 `/teams` · `/draft` · `/result`
- **종료:** Setup 삭제 또는 새 내전 시작 → `POST .../cancel` (`CANCELLED`). 이미 저장된 경기 결과·전적 집계는 유지

| `CUSTOM_GAME.STATUS` | 이어가기 목적지 |
|----------------------|-----------------|
| `PLAYERS` | `/teams` |
| `TEAM_SETUP`, `DRAFT` | `/draft` |
| `RESULT_INPUT` | `/result` |
| `FINISHED`, `CANCELLED` | 이어가기 불가 (404) |

### 세션 이어서 하기 — 수동 검증 (5단계)

사전: `npm run server` + Oracle, 프론트 dev 서버 실행.

| # | 시나리오 | 기대 결과 |
|---|----------|-----------|
| 1 | 로스터만 저장 → localStorage 삭제 → Setup에서 세션 코드로 이어하기 | `/teams`, 10명 복구 |
| 2 | 팀 배치까지 저장 → 이어하기 | `/draft`, 5:5·라인(탑~서폿) 유지 |
| 3 | 밴픽 마감 후 → 이어하기 | `/result`, 픽 초상화 표시 |
| 4 | BO3 등 1판 결과 저장 후 → 이어하기 | `/draft`, 2판·스코어·피어리스 사용 챔프 유지 |
| 5 | Setup 「진행 중 내전 삭제」 후 같은 코드로 이어하기 | 실패(종료된 세션). `CUSTOM_MATCH_PLAYER_RESULT`·전적은 유지 |

DB 확인 예시 (`c##lol_scrim`):

```sql
SELECT SESSION_CODE, STATUS, CURRENT_MATCH_NO, RED_SERIES_WINS, BLUE_SERIES_WINS
FROM CUSTOM_GAME ORDER BY UPDATED_AT DESC;

SELECT SUMMONER_ID, TEAM_COLOR, POSITION_NAME, SORT_ORDER
FROM CUSTOM_GAME_PLAYER WHERE GAME_ID = :gameId ORDER BY SORT_ORDER;
```

### 기타

- Riot 개발용 키 **24시간 만료**, rate limit → 캐시·순차 조회
- API Key는 **브라우저 저장 최소화**, 실제 호출은 **서버**에서
- 내전 결과·밴픽은 **자체 히스토리**만 신뢰 (공식 match-v5와 무관)
- 소환사 입력 화면 전적: **내전 0판 → 랭크**, **내전 1판+ → 내전 DB 집계** (Riot API는 랭크 fallback·소환사 조회용)

---

*원본: `기획.rtf` · 갱신: Oracle REST·세션 이어서 하기·STATUS 전이·검증 시나리오 반영 (`backend/README.md` API 목록)*
