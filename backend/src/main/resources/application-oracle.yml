spring:
  autoconfigure:
    exclude: []
  datasource:
    # SQL Developer: SID=xe (Service Name 아님)
    url: ${ORACLE_JDBC_URL:jdbc:oracle:thin:@localhost:1521:xe}
    username: "c##lol_scrim"
    password: ${ORACLE_PASSWORD:}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul
